REPORTER = rspec

test-all:
	./node_modules/.bin/mocha --colors --reporter spec --compilers coffee:coffee-script --ignore-leaks test/*.coffee test/**/*.coffee

test:
	./node_modules/.bin/mocha --colors --reporter spec --compilers coffee:coffee-script --ignore-leaks $(file)

.PHONY: test

