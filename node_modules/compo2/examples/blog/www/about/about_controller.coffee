@message = 'Hello world!'

@render()   # Renders the default view, with the data defined in this controller.

#@render status: 201    # Renders the default view, with the given response code.
#@render 'another view'   # Renders the given view, with the data defined in this controller.
#@render 'another view', status: 201
#@render json: foo: "bar"   # Renders a JSON response with the given data.

