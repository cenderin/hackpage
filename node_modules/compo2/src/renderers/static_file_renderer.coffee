# Renderer for a static file

fs = require 'fs'

module.exports = class StaticFileRenderer
  constructor: (@path) ->

  render: (res, data, done) ->
    fs.readFile @path, (err, data) ->
      return console.log err if err
      res.write data
      done()

