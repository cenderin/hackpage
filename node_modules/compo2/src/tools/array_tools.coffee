# Useful helper methods for dealing with arrays.
class ArrayTools

  # Calls the given function on each element of the array
  # Returns true if it returns true for any element in the array
  # See http://ruby-doc.org/core-1.9.3/Enumerable.html#method-i-any-3F
  @any: (array, func) ->
    (func = (element) -> element) unless func?
    for element in array
      return true if func(element)
    false


  # Returns the first element from the array for which
  # the given checker function returns a truthy value.
  @detect: (array, checker) ->
    return element for element in array when checker(element)



module.exports = ArrayTools
