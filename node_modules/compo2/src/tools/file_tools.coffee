# Utilities for file management.

fs = require 'fs'

module.exports = class FileTools

  # Calls the given callback if the given path points to  file.
  @ifDir: (path, callback) ->
    fs.stat path, (err, stats) ->
      return console.log err if err
      return unless stats.isFile()
      callback()

