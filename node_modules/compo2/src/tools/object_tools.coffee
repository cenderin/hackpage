class ObjectTools

  # Returns the number of elements (key-value pairs) of the given object.
  @size: (obj) ->
    result = 0
    result++ for own key, value of obj
    result



module.exports = ObjectTools
