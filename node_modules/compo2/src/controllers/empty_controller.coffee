# Dummy controller instance for sections that have no controller.
class EmptyController

  # The controller method.
  #
  # Called on each request. Runs the controller logic.
  execute: (request, response, params, callback) ->
    callback { params: params }


module.exports = EmptyController
