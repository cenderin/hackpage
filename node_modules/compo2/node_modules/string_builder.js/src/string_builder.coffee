# Structured API for building up a JS string in small steps.
class StringBuilder

  constructor: ->
    @content = []

  append: (text) =>
    @content.push text
    @

  toString: =>
    @content.join ''


module.exports = StringBuilder
