[expect, sinon] = require('../test_helper')
HttpTools = require '../../src/tools/http_tools.coffee'


describe 'HttpTools', ->

  describe 'get', ->

    # NOTE(KG): This test has external dependencies.
    #           It is disabled by default.
    #           Enable it manually if you want to test this method.
    #it 'downloads the data from the given URL', (done) ->
      #HttpTools.get 'http://www.google.com', (body, status) ->
      #  expect(status).to.equal 200
      #  console.log body
      #  done()


  describe 'request', ->

    #it 'performs the given request', (done) ->
    #  HttpTools.request 'GET', 'www.google.com', 80, '', '', (body, status) ->
    #    expect(status).to.equal 200
    #    done()
